# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (for storing app configs)
CONFIG_DB_PATH=./data/config.db

# Backups Storage
BACKUP_DIR=./backups

# Docker Socket (default for most systems)
DOCKER_SOCKET=/var/run/docker.sock
